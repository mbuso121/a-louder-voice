// This file is kept for reference but authRoutes.js + authController.js is the active implementation
// Both register and login are handled via /routes/authRoutes.js -> /controllers/authController.js
import express from "express";
const router = express.Router();
export default router;
